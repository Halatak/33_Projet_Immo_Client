import { BienImmobilierLocation } from './bien-immobilier-location';

describe('BienImmobilierLocation', () => {
  it('should create an instance', () => {
    expect(new BienImmobilierLocation()).toBeTruthy();
  });
});
